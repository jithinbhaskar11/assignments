import 'package:flutter/material.dart';

class Gmail_Verification_Home extends StatelessWidget {
  const Gmail_Verification_Home({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gmail home'),),
    );
  }
}
